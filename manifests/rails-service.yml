apiVersion: v1
kind: Service
metadata:
  name: rails-service
spec:
  selector:
    app: rails-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 32080   # You can pick any port between 30000-32767
  type: NodePort

